components:
  schemas:
    Endpoint:
      type: object
      properties:
        name:
          type: string
          example: client-1
          nullable: false
        ip:
          type: string
          format: ipv4
          nullable: false
        host:
          type: string
          format: hostname
          nullable: true