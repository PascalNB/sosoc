components:
  schemas:
    Endpoint:
      type: object
      properties:
        name:
          type: string
          example: client-1
        ip:
          type: string
          format: ipv4
        host:
          type: string
          format: hostname
          nullable: true