components:
  schemas:
    Workflow:
      type: object
      properties:
        id:
          type: string
          format: uuid
          nullable: false
        alarm:
          $ref: './alarms.yaml#/components/schemas/Alarm'
          nullable: false
        playbook:
          $ref: './playbooks.yaml#/components/schemas/Playbook'
          nullable: false
        nextStep:
          type: string
          format: uuid
          nullable: true