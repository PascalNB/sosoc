components:
  schemas:
    IOC:
      type: object
      properties:
        id:
          type: string
          format: uuid
        threat:
          $ref: './alarms.yaml#/components/schemas/Threat'
        match:
          type: string
          format: SpEL
          example: Data['email']['source'] == malicious.com