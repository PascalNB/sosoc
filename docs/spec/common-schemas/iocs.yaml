components:
  schemas:
    IOC:
      type: object
      properties:
        id:
          type: string
          format: uuid
        threat:
          $ref: './alarms.yaml#/components/schemas/Threat'
        indicators:
          type: object
          additionalProperties: true