components:
  schemas:
    Action:
      type: object
      properties:
        user:
          $ref: './users.yaml#/components/schemas/User'
        action:
          type: object
          properties:
            code:
              type: string
              nullable: false
            data:
              type: object
              additionalProperties: true
              nullable: false