apiVersion: apps/v1
kind: Deployment
metadata:
  name: playbookmanagement
  namespace: soa-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: playbookmanagement
  template:
    metadata:
      labels:
        app: playbookmanagement
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command:
            - sh
            - -c
            - |
              until nc -z sosoc-db 5432; do
                echo "Waiting for DB"; sleep 2; 
              done;
      containers:
        - name: playbookmanagement
          image: playbookmanagement:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: playbookmanagement
  namespace: soa-project
spec:
  type: ClusterIP
  selector:
    app: playbookmanagement
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

