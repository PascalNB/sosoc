apiVersion: apps/v1
kind: Deployment
metadata:
  name: threatintelligence
  namespace: soa-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: threatintelligence
  template:
    metadata:
      labels:
        app: threatintelligence
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command:
            - sh
            - -c
            - |
              until nc -z sosoc-db 5432; do
                echo "Waiting for DB"; sleep 2; 
              done;
      containers:
        - name: threatintelligence
          image: threatintelligence:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: threatintelligence
  namespace: soa-project
spec:
  type: ClusterIP
  selector:
    app: threatintelligence
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

