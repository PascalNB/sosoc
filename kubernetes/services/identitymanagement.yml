apiVersion: apps/v1
kind: Deployment
metadata:
  name: identitymanagement
  namespace: soa-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: identitymanagement
  template:
    metadata:
      labels:
        app: identitymanagement
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command:
            - sh
            - -c
            - |
              until nc -z sosoc-db 5432; do
                echo "Waiting for DB"; sleep 2; 
              done;
      containers:
        - name: identitymanagement
          image: identitymanagement:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: identitymanagement
  namespace: soa-project
spec:
  type: ClusterIP
  selector:
    app: identitymanagement
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

